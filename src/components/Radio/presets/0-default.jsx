import React from 'react';
import Radio from '../Radio';

export default (
    <Radio
    uxpId='action1'
    checked={false}
    onChange={()=>undefined}
    label="Let's Merge!"
    >
    </Radio>
)